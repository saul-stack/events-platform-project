{
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "scripts": {
    "dev": "node ./seed-test-tables.js && concurrently -n \"Vite,Express,SCSS\" \"npm run host --prefix frontend\" \"npm run start --prefix backend\" \"npm run watch-css --prefix frontend\""
  }
}
